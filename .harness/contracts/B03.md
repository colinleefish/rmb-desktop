# Sprint Contract — B03: tray update baseline

- **Bug ID**: B03 (bug_list.json)
- **Issue**: #15
- **Branch / worktree**: `fix/B03-tray-update-baseline` / `../rmb-desktop-fix-b03`
- **Date**: 2026-09-12

## Scope

**In**:
- Compare update feed against installed sidecar version (stamp in `~/.rmb/bin` after `update.Apply`, daemon `/api/v1/version` fallback, else shell `version.Version`).
- Regression test `// B03`, investigation doc, bug_list regression metadata.
- Write sidecar version stamp on successful apply.

**Out**:
- Shell/DMG self-update.
- Tray UI polish beyond using the corrected baseline in existing check paths.
- Backfill stamp for installs that never re-run Apply (daemon fallback covers them).

## Definition of Done

`make verify-bug B=B03` passes (L1/L2/L3). Tray logic uses `updateCheckVersion` for `update.Check`.

## Risks / open questions

- Daemon fallback when stamp missing but sidecars newer than shell (pre-fix installs).

## Timebox

- One session (investigation + fix).
