# Sprint Contract — B05: incumbent materiality read-skew race

> Bug fix session (B05 / issue #72). Scope locked to the concurrent materiality gate race only.

- **Bug ID**: B05
- **Issue**: #72
- **Branch / worktree**: `fix/B05-incumbent-materiality-race` / `../rmb-desktop-fix-b05`
- **Date**: 2026-09-12

## Scope

**In** (this contract delivers):
- Deterministic `// B05` regression reproducing merge-before-materiality-read interleaving
- Fix for double version bump on incumbent URI when variant merges concurrently in the same rollup
- `docs/bugs/B05-investigation.md`, `bug_list.json` regression metadata, PR + issue comment

**Out** (explicitly excluded — do not touch):
- Unrelated worker refactors, L3 concurrency tuning, embedder/incumbent detection changes
- Broader materiality semantics (content-hash vs atom-id) beyond this race

## Definition of Done

`make verify-bug B=B05` passes (L1/L2/L3). Regression fails on unfixed code for the right reason.

## Risks / open questions

- Per-URI lock scope must cover merge-into-incumbent targets, not only bucket.URI

## Timebox

- Planned: one session. If exceeded: checkpoint PROGRESS.md with branch state.
