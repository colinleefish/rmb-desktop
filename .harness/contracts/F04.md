# Sprint Contract — F04: webui-shell-restructure

- **Feature ID**: F04 (feature_list.json)
- **Issue**: — (Refactor A / information architecture)
- **Branch / worktree**: `feat/webui-ia-shell` / `../rmb-desktop-webui-ia-shell`
- **Date**: 2026-09-12

## Scope

**In** (this contract delivers):
- Flat job-oriented sidebar: Overview · Sessions · Memories · Agents · Settings.
- Global recall bar (⌘K) in Topbar; Enter goes to `/memories?q=…`.
- rmbd heartbeat in sidebar footer (mock vs local vs down).
- Memories in-page tabs (All / Profile / Events / Preferences / Entities / Corrections); no duplicate in-page search.
- Agents hub with Integration + Skills tabs; Skills nested under `/agents/skills`.
- Legacy redirects from `/skills` and `/integrations` (and `settings/integrations/*`) onto the new routes — **`settings/integrations` redirect lives in `App.tsx`**, not a dedicated page component.
- F10 `*Mock.ts` files kept.

**Out** (explicitly excluded — do not touch):
- SettingsPage split (F05 / Refactor B).
- Deleting F10 mock files or a visual redesign.
- Master-detail session pane.
- Hand-edits under `internal/http/static/web/`.

## Definition of Done

The feature's `layers` in `feature_list.json` (L1 static / L2 runtime / L3 system) all pass via
`make verify-feature F=F04`. No hand-waved completion: state moves to `passing` only with evidence.

## Risks / open questions

- Q1–Q4 closed from the wireframe: global ⌘K is Memories search; Corrections is a Memories tab; Skills is an Agents tab; session detail stays a route.

## Timebox

- Planned: one session. If exceeded: checkpoint in PROGRESS.md, stop, renegotiate.
