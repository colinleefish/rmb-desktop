# Sprint Contract — F08: bug-workflow-track

> Fill this BEFORE starting implementation. One feature = one contract = one session (L07/L10).
> Negotiate scope with the user; record it here; drift means stop-and-renegotiate, not silently expand.

- **Feature ID**: F08 (feature_list.json)
- **Issue**: #63
- **Branch / worktree**: `feat/bug-workflow` / `../rmb-desktop-bug-workflow`
- **Date**: 2026-08-29

## Scope

**In** (this contract delivers):
- `bug_list.json` + `bug_list.schema.json` (five-state machine: reported → investigating → diagnosed → fixing → passing)
- `scripts/verify-bug.sh` + `make verify-bug` / `make bug-state` targets with gated transitions:
  - diagnosed gate actually runs the regression test and requires it to FAIL on unfixed code
  - fixing gate requires the bug branch to exist and enforces bug WIP=1
- `AGENTS.md` Bug workflow section (phases, gates, scope↔module map, flaky subtype)
- `templates/bug-report.md`, `templates/bug-investigation.md`, `.github/ISSUE_TEMPLATE/bug.md`, `docs/bugs/README.md`
- `scripts/clean-state-check.sh` D3 extended to validate bug_list.json; `templates/clean-state-checklist.md` wording updated
- `.claude/settings.json`: `gh issue/label` + `scripts/with-proxy.sh` permissions
- Backfill open bugs as `reported`: B01 = #61, B02 = #62 (both pointing at the existing investigation at `docs/audit/2026-08-29-zcode-capture-bugs/`), B03 = #15

**Out** (explicitly excluded — do not touch):
- PR CI gate + branch protection (F09 / #64, separate branch)
- Fixing #61/#62/#15 themselves (fix worktrees `../rmb-desktop-fix-61` / `-fix-62` already in flight)
- Webui verify gate (#60) and release-pipeline CI (#40)
- Advancing B01–B03 past `reported` (that is the dogfood follow-up once regression tests exist on their branches)

## Definition of Done

The feature's `layers` in `feature_list.json` (L1 static / L2 runtime / L3 system) all pass via
`make verify-feature F=F08`. No hand-waved completion: state moves to `passing` only with evidence.

## Risks / open questions

- verify-bug gates run in the caller's checkout — documented that `bug-state` must be invoked inside the bug's worktree (same convention as verify-feature).
- The diagnosed gate executes the regression cmd from bug_list.json; a cmd that fails to compile also exits non-zero, so the gate additionally requires `FAIL` in the output to distinguish "test failed" from "build broke".
- bug_list.json is edited by multiple in-flight bug branches — merge conflicts are accepted (same property as feature_list.json).

## Timebox

- Planned: one session (paired with F09 in the same clock-in). If exceeded: checkpoint in PROGRESS.md, stop, renegotiate.
