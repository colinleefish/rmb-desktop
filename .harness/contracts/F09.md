# Sprint Contract — F09: pr-ci-gate

> Fill this BEFORE starting implementation. One feature = one contract = one session (L07/L10).

- **Feature ID**: F09 (feature_list.json)
- **Issue**: #64
- **Branch / worktree**: `feat/pr-ci-gate` / `../rmb-desktop-pr-ci-gate`
- **Date**: 2026-08-29

## Scope

**In** (this contract delivers):
- `.github/workflows/pr-check.yml`: full local parity — `make setup` → `make webui-build` → `make check` on ubuntu-latest
  (CGO_ENABLED=1 + sqlite_fts5 + gcc, jq preinstalled). Triggers: every PR (no path filter) + push to main.
  `setup-go cache: false` (issue #40 toolchain tar race) + explicit actions/cache for `~/.cache/go-build` keyed on go.mod/go.sum
  + setup-node npm cache. concurrency cancel-in-progress, 15-min timeout, `permissions: contents: read`.
- Branch protection on main (after this branch merges): require status check `check`, require PRs (0 approvals),
  merge-commit only (squash/rebase disabled), admins may bypass.
- Docs: AGENTS.md Bug workflow phase 3 references the PR gate (landed with F08 wording); this commit updates
  `plan/parallel-work-and-versioning.md` §2.4 with the PR+CI merge path; `.claude/settings.json` gains `gh pr/run/api`.

**Out** (explicitly excluded — do not touch):
- `make check` compiling/linting the SPA itself (#60 — `chore/webui-verify-gate` worktree exists)
- Release pipeline CI (#40 stays open; only the same setup-go race avoidance is borrowed)
- macOS/Windows CI matrices (Linux-only for now; cross-platform release pipeline is out of scope)

## Definition of Done

The feature's `layers` in `feature_list.json` all pass via `make verify-feature F=F09`.
L2 = a real green `pr-check` run on this branch's PR. L3 = `make check` green AND branch protection API
returns required status checks on main.

## Risks / open questions

- The `check` status context name must match the job name exactly — confirmed from the actual green run before wiring protection.
- First run pays cold-cache costs (npm ci + CGO build); later runs use the two caches (~2–3 min expected).
- This feature's own merge is the last direct push to main before protection is switched on.

## Timebox

- Planned: one session (paired with F08 in the same clock-in). If exceeded: checkpoint in PROGRESS.md, stop, renegotiate.
