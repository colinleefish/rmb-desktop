# Sprint Contract — F11: webui-verify-gate

- **Feature ID**: F11 (feature_list.json)
- **Issue**: #60
- **Branch / worktree**: `chore/webui-verify-gate` / `../rmb-desktop-chore-webui-verify`
- **Date**: 2026-09-12

## Scope

**In** (this contract delivers):
- Makefile target `webui-verify`: `pnpm run lint` + `pnpm run build` in `webui/` (typecheck via `tsc -b` inside build).
- `make check` depends on `webui-verify` before `webui-embed-check` so TS/lint breakage in `webui/src` fails the predicate even when a stale embed exists.
- Feature F11 in `feature_list.json` with L1 tsc+oxlint, L2 `make webui-verify`, L3 `make check`.
- AGENTS.md verification line + `docs/quality-document.md` harness/webui notes.

**Out** (explicitly excluded):
- No hand-edits under `internal/http/static/web/`; embed refresh remains `make webui-build` only.
- No vitest/component/e2e test suite addition (quality grade for webui tests stays C).
- No oxlint warning cleanup beyond trivial (<20) — four `only-export-components` warnings remain (non-failing).

## Definition of Done

All F11 layers pass via `make verify-feature F=F11`. Acceptance: a type error in `webui/src/App.tsx` turns `make check` red without requiring `webui-build`.

## Risks / open questions

- PR CI (`pr-check`) runs `webui-build` then `make check` — webui is built twice per run (acceptable; dedupe is a future optimization).

## Timebox

- Planned: one session.
