# Sprint Contract — F12: drop-corrections-tab

- **Feature ID**: F12 (feature_list.json)
- **Issue**: —
- **Branch / worktree**: `feat/drop-corrections-tab` / `../rmb-desktop-drop-corrections-tab`
- **Date**: 2026-09-12

## Scope

**In** (this contract delivers):
- Remove the Memories **Corrections** tab so overrides are not a sibling of Profile / Events / Preferences / Entities.
- Keep add / retract on the target memory detail (`MemoryCorrections` in the list modal and on the profile page).
- `/memories/corrections` falls through as an unknown category → `/memories`.
- Record the product decision: corrections stay distill-time pins (not auto-absorbed on version bump).

**Out** (explicitly excluded — do not touch):
- Correction table, HTTP API, L3 prompt injection, `retracted_at` semantics.
- Auto-absorb / auto-retract after re-distill (considered and rejected).
- Overview corrections count.
- Hand-edits under `internal/http/static/web/`.

## Definition of Done

The feature's `layers` in `feature_list.json` (L1 static / L2 runtime / L3 system) all pass via
`make verify-feature F=F12`. No hand-waved completion: state moves to `passing` only with evidence.

## Risks / open questions

- Old bookmarks to `/memories/corrections` silently land on All — acceptable.
- F04's original sprint shipped the tab; this follow-up supersedes that slice of IA without rewriting F04's historical DoD.

## Timebox

- Planned: one session.
