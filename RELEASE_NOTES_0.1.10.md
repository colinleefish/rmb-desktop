## Settings & tray

- Tray health check reads daemon address from config (fixes "Starting…" after changing port).
- Launch at login now applies with Save instead of immediately.
- Settings success/error messages clear when switching tabs.
- Embedding model/base/dimensions changes show a confirmation dialog before save.
- API key fields show masked placeholder when a key is already configured.

## Other

- Exclude rmb://agent singleton from memory counts in overview.
- Simplify LLM connection test response fields.
