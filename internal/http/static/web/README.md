# Embedded web UI (generated)

This directory is populated by `make webui-build` (copies `webui/dist/` here for `//go:embed`).

It is **not** checked into git. After clone, run:

```bash
make build-all   # webui-build + go build
```

Or `make webui-build` before `make build`.
